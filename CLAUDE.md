# e2e-architect plugin

Business-aware E2E test generation. Understands your SaaS product before writing tests.

## Skills

- **e2e-architect** — Generate, audit, and improve E2E tests that validate real business flows. Three modes: `discover` (scan repo, build flow map), `generate` (create Playwright tests), `audit` (classify existing tests).
